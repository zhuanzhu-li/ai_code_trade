# 数据库配置文件
# 请根据你的实际情况修改以下配置

# 方式1: 使用DATABASE_URL (推荐)
# 格式: mysql+pymysql://用户名:密码@主机:端口/数据库名
DATABASE_URL=mysql+pymysql://root:@localhost:3306/quant_trading
DEV_DATABASE_URL=mysql+pymysql://root:@localhost:3306/quant_trading_dev

# 方式2: 使用独立的数据库配置变量
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=quant_trading

# 注意事项:
# 1. 如果MySQL有密码，请在@符号前添加密码，例如: root:your_password@localhost
# 2. 确保MySQL服务正在运行
# 3. 确保用户有足够的权限创建数据库和表
# 4. 如果使用非默认端口，请修改端口号

# 安全配置
SECRET_KEY=your-secret-key-here

# 交易配置
TRADING_ENABLED=False
MAX_POSITION_SIZE=10000
MAX_DAILY_LOSS=1000

# API配置
API_RATE_LIMIT=1000

# 交易所API密钥
BINANCE_API_KEY=your_binance_api_key
BINANCE_SECRET_KEY=your_binance_secret_key

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=logs/trading.log
